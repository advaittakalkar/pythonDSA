# Naive approach --> take another temp array and copy elements to old array in reverse order

def reverseArray(numList):
    print(numList[::-1])


if __name__ == "__main__":
    numList = [1, 2, 3, 4]
    reverseArray(numList)


